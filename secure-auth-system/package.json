{
	"name": "secure-auth-system",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"install:all": "cd server && npm install && cd ../client && npm install",
		"dev": "echo \"🫣 Wrong folder?: 'npm run dev' should be run in ./client directory\" && echo \"💡 Try: 'cd client && npm run dev'\" && exit 1",
		"server": "echo \"🫣 Wrong folder?: 'npm run start' should be run in ./server directory\" && echo \"💡 Try: 'cd server && npm run start'\" && exit 1"
	}
}
